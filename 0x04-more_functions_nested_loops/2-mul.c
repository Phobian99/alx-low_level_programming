#include "main.h"

/**
 *mul - multiplies two integers
 * @a: a - Variable
 * @b: b - variable
 * Return: Always 0.
 */
int _isdigit(int c)
{
	int (a);
	int (b);
	int (c);

	int c = a * b;
		return (c);
		
